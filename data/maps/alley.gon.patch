nodes.append {
    //new exits added to alley
    exit_mansion {
        type exit
        next_map mansion.gon
        locked true
        override_art MapNodeExit_Mansion
    }
}

flags.append {
	mapflag_SewersUnlocked { //flag that controls when new exit is unlocked (using sewers for demonstration purposes)
        exit_mansion {
            locked false
        }
    }
}
